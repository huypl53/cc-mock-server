"""cc-mock-server: smart mocking proxy for AI coding agents.

See README.md for usage. The `cc-mock` CLI entry point (phase 7) will
replace `_cli_placeholder` below once the control API and proxy are wired
up in `app.py`.
"""

__version__ = "0.1.0"


def _cli_placeholder() -> None:  # pragma: no cover - implemented in phase 7
    raise NotImplementedError(
        "cc-mock CLI is not implemented yet (see phase 7: Control API & CLI)."
    )
